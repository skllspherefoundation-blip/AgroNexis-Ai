{
  "name": "agronexis-ai-workspace",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix agronexis-ai run dev",
    "build": "npm --prefix agronexis-ai run build",
    "start": "npm --prefix agronexis-ai run start",
    "lint": "npm --prefix agronexis-ai run lint"
  }
}
